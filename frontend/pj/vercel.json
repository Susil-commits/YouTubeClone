{
  "version": 2,
  "installCommand": "npm install --no-optional",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
