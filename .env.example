# Backend Configuration
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/dbname
JWT_SECRET=your_jwt_secret_key_here
PORT=4000

# Frontend Configuration (for local dev)
# On Vercel, this is automatically handled if you set the Environment Variable in Vercel settings
VITE_API_URL=/api
