{
  "version": 2,
  "installCommand": "if [ -d \"frontend/pj\" ]; then cd frontend/pj && npm install --no-optional; else npm install --no-optional; fi",
  "builds": [
    {
      "src": "frontend/pj/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    },
    {
      "src": "server/src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://youtubeclone-d2s7.onrender.com/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "^/(.*)",
      "dest": "/index.html"
    }
  ]
}

